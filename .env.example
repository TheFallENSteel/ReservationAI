# Neon Postgres connection string, e.g. from the Neon dashboard.
# Required in production (Vercel). If unset locally, the API falls back to in-memory mock data.
DATABASE_URL=postgres://user:password@ep-example-000000.us-east-2.aws.neon.tech/neondb?sslmode=require
PORT=3000
