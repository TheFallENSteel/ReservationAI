# Neon Postgres connection string, e.g. from the Neon dashboard.
# Required in production (Vercel). If unset locally, the API falls back to in-memory mock data.
DATABASE_URL="<mockurl>"
PORT=3000

SMTP_HOST="smtp.resend.com"
SMTP_PORT=465
SMTP_USER="resend"
SMTP_PASS="<yourpass>"
EMAIL_FROM="Reservation <noreply@komarem.cz>"
APP_URL="https://reservation-ai-five.vercel.app"